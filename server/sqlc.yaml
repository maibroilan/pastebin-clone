version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query"
    schema: "sql/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"

        emit_pointers_for_null_types: true
        emit_json_tags: true
        emit_interface: false
